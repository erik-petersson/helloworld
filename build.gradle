buildscript {
    repositories {
        mavenCentral()
		jcenter()
    }
	
	dependencies {
		classpath 'org.sonarsource.scanner.gradle:sonarqube-gradle-plugin:2.5'
		classpath 'info.solidsoft.gradle.pitest:gradle-pitest-plugin:1.1.11'
        classpath "org.jfrog.buildinfo:build-info-extractor-gradle:4+"
	}
}

plugins {
  id 'org.sonarqube' version '2.5'
  id 'jacoco'
  id 'java'
}

apply plugin: 'info.solidsoft.pitest'
apply plugin: "com.jfrog.artifactory"
apply plugin: "maven-publish"


version = '1.0.0'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
	jcenter()
	mavenLocal()
}

ext {
    junitVersion = '4.12'
}

dependencies {
	
	
    testCompile group: 'junit', name: 'junit', version: junitVersion

}


pitest {

    targetClasses = ['helloworld.*']

    threads = 1
    outputFormats = ['XML']
}


sonarqube {
  properties {
    property "sonar.projectName", "Hello world"
    property "sonar.projectKey", "org.sonarqube:java-gradle-simple"
    property "sonar.pitest.mode", "reuseReport"
    property "sonar.pitest.reportsDirectory", "build"
  }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}

artifactory {
    contextUrl = "http://artifactory:8081/artifactory"   //The base Artifactory URL if not overridden by the publisher/resolver
    publish {
        repository {
            repoKey = 'gradle-dev-local'
            username = "${artifactory_user}"
            password = "${artifactory_password}"
            maven = true
            
        }
        defaults {
            publications('mavenJava')
            publishArtifacts = true
            publishPom = true
        }
    }
    resolve {
        repository {
            repoKey = 'gradle-dev'
            username = "${artifactory_user}"
            password = "${artifactory_password}"
            maven = true
            
        }
    }
}